# Platform

Android and iOS boundary adapters for WebView, permissions, downloads, lifecycle, and platform channels. Platform APIs must be exposed through Application ports.

Suggested modules: `webview/`, `permissions/`, `downloads/`, `lifecycle/`, and `channels/`.
