# Application

Use cases, application ports, DTOs, orchestration, and policy-independent validation. This layer may depend only on Domain.

Suggested modules: `use_cases/`, `ports/`, `dto/`, `commands/`, and `queries/`.
