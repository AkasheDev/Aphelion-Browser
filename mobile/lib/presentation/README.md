# Presentation

Flutter UI, navigation, state holders, themes, pages, and reusable widgets. Business rules must remain in Domain or Application.

Suggested modules: `app/`, `navigation/`, `state/`, `pages/`, `widgets/`, and `theme/`.
