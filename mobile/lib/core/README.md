# Core

Small cross-cutting primitives shared by multiple layers: configuration contracts, typed failures, logging abstractions, constants, and utilities. Core must not become a miscellaneous business-logic folder.

Suggested modules: `config/`, `errors/`, `logging/`, `security/`, and `utils/`.
