# Infrastructure

Adapters for persistence, remote APIs, secure storage, synchronization, telemetry, and repository implementations. This layer implements Application and Domain contracts.

Suggested modules: `persistence/`, `network/`, `security/`, `repositories/`, `sync/`, and `telemetry/`.
