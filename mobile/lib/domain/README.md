# Domain

Pure Dart enterprise rules, entities, value objects, repository contracts, and domain services. This layer must not import Flutter, infrastructure, presentation, or platform code.

Suggested modules: `entities/`, `value_objects/`, `repositories/`, `services/`, and `events/`.
